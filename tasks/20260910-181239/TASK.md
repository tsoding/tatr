# `tatr ls -debug` should be a separate command

- TAGS: scope
- PRIORITY: 100
- STATUS: OPEN

Since now we have two commands that accept TQL. So it only makes sense that query debugging is a separate command.
